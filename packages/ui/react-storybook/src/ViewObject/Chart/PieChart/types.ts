import type { CanvasLayerInfo } from '@src/ViewObject/Canvas/types';

type PieChartParam = {
  nodeId: string;
  width: number;
  height: number;
  canvasLayerInfo: Array<CanvasLayerInfo>;
};

export { PieChartParam };
